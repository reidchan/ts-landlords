{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "dependencies": {
    "egg-scripts": "^2.10.0",
    "egg-socket.io": "^4.1.6",
    "midway": "^1.0.0"
  },
  "devDependencies": {
    "@types/mocha": "^5.2.7",
    "@types/node": "^10.5.5",
    "@waiting/eslint-config": "^1.10.2",
    "cross-env": "^5.2.0",
    "egg-ci": "^1.8.0",
    "egg-redis": "^2.4.0",
    "eslint": "^6.0.0",
    "espower-typescript": "^9.0.2",
    "ioredis": "^4.14.1",
    "midway-bin": "1",
    "midway-mock": "1",
    "mocha": "^6.2.0",
    "mysql": "^2.17.1",
    "reflect-metadata": "^0.1.13",
    "ts-node": "^8.3.0",
    "tslib": "^1.8.1",
    "typeorm": "^0.2.22",
    "typescript": "^3.5.0"
  },
  "engines": {
    "node": ">=10.16.0"
  },
  "scripts": {
    "start": "egg-scripts start --daemon --title=egg-server-backend --framework=midway --ts",
    "stop": "egg-scripts stop --title=egg-server-backend",
    "start_build": "npm run build && cross-env NODE_ENV=development midway-bin dev",
    "clean": "midway-bin clean",
    "dev": "cross-env NODE_ENV=local midway-bin dev --ts",
    "debug": "cross-env NODE_ENV=local midway-bin debug --ts",
    "test": "npm run lint && cross-env TS_NODE_PROJECT=test/tsconfig.json midway-bin test --ts",
    "cov": "midway-bin cov --ts",
    "lint": "eslint --fix {src,test}/**/*.ts",
    "ci": "npm run cov",
    "build": "midway-bin build -c"
  },
  "ci": {
    "version": "8, 10"
  },
  "midway-bin-clean": [
    ".vscode/.tsbuildinfo",
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": ""
  },
  "author": "super2god",
  "license": "MIT"
}
